# Integration Hub base URL (used by Vite dev proxy)
VITE_API_BASE_URL=http://localhost:3000

# Dev harness config — mirrors what a host sets via window.TOS_CONFIG
VITE_DEV_TENANT_ID=dev-tenant
VITE_DEV_PAGE_SLUG=home
